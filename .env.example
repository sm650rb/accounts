# MySQL (StackCP — use internal hostname on-server, remote gateway for off-server)
DB_HOST=sdb-70.hosting.stackcp.net
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# Public URL (email links, Google OAuth redirect)
APP_URL=https://acc.sm650.com

# Min 32 random characters
SESSION_SECRET=replace-with-a-long-random-string-at-least-32-chars

# Dev OTP (local only — remove in production)
# DEV_OTP=000000

# Gmail SMTP (Google App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=sm650rb@gmail.com
SMTP_PASSWORD=your-google-app-password
SMTP_FROM="Road Burners <sm650rb@gmail.com>"

# Google SSO — redirect: https://acc.sm650.com/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
